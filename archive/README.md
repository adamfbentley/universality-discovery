# Archive

This directory contains older project artifacts that are kept for provenance but
should not be read as the current project framing.

The manuscript drafts in `obsolete_manuscripts/` were written before later
diagnostics clarified several limitations of the method. The current project
interpretation is in the root `README.md` and `docs/EXPERIMENT_LOG.md`.
